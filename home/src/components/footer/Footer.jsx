import React from 'react'
import PropTypes from 'prop-types'
import styles from './footer.module.scss';

function Footer(props) {
  return (
    <div className={styles.footer}>Only The Best Spinner</div>
  )
}

Footer.propTypes = {}

export default Footer
